name: Enterprise CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  sonar-scan:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run SonarQube Scan
        run: sonar-scanner